rules_version = '2';

service cloud.firestore {
    match /databases/{database}/documents {
        // TEST
        match /sumn1/{sumnID} {
            allow read, write: if request.auth != null;
        }
    }
}