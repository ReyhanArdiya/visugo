import { theme } from "@chakra-ui/react";

const fonts = {
    title: `"PT Sans Narrow", ${theme.fonts.heading}`,
    heading: `"Julius Sans One", ${theme.fonts.heading}`,
    body: `"Palanquin", ${theme.fonts.body}`,
    sans: `Fira Sans Extra Condensed, ${theme.fonts.body}`,
};

export default fonts;
