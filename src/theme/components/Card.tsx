import { ComponentStyleConfig } from "@chakra-ui/react";

const Card: ComponentStyleConfig = {
    baseStyle: {
        shadow: "lg",
        rounded: "md",
    },
    defaultProps: {
        size: null,
        variant: null,
    },
};

export default Card;
